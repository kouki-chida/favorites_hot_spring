  <% if hot_spring.favorited_by?(current_user) %>
    <%= link_to hot_spring.favorites.count, hot_spring_favorites_path(hot_spring), method: :delete, remote: true, class: "fas fa-heart heart_red" %>
  <% else %>
    <%= link_to hot_spring.favorites.count, hot_spring_favorites_path(hot_spring), method: :post, remote: true, class: "fas fa-heart heart_blue" %>
  <% end %>